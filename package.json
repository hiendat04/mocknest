{
  "name": "mocknest",
  "private": true,
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "packages/*",
    "extension",
    "dashboard"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
